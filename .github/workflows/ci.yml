name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: [18.x, 20.x]
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Lint
        run: npm run lint:ci
      
      - name: Type check
        run: npm run typecheck
      
      - name: Run unit tests
        run: npm run test:unit
      
      - name: Run unit tests with coverage
        run: npm run test:coverage -- --run tests/unit
      
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report-node-${{ matrix.node-version }}
          path: coverage/
      
      # Integration tests are disabled by default in CI since they need an API key
      # Uncomment if you want to run them in CI and add the RECLAIM_API_KEY secret
      # - name: Run integration tests
      #   if: ${{ secrets.RECLAIM_API_KEY }}
      #   run: npm run test:integration
      #   env:
      #     RECLAIM_API_KEY: ${{ secrets.RECLAIM_API_KEY }}